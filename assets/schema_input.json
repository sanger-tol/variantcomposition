{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://raw.githubusercontent.com/sanger-tol/variantcomposition/main/assets/schema_input.json",
    "title": "sanger-tol/variantcomposition pipeline - params.input schema",
    "description": "Schema for the file provided with params.input",
    "type": "array",
    "items": {
        "type": "object",
        "properties": {
            "sample": {
                "type": "string",
                "pattern": "^[a-zA-Z][^\\s/]*(?:/[a-zA-Z][^\\s/]*)*$",
                "errorMessage": "Sample name must be provided, cannot contain spaces, and uses '/' to separate the levels of the output directory hierarchy (e.g. 'specimen', 'specimen/run' or 'pacbio/specimen/run')",
                "meta": ["sample"]
            },
            "datatype": {
                "type": "string",
                "pattern": "^\\S+$",
                "errorMessage": "Data type, and must be one of: 'vcf', 'gvcf', 'bcf', 'gbcf', 'af'",
                "enum": ["vcf", "gvcf", "bcf", "gbcf", "af"],
                "meta": ["datatype"]
            },
            "datafile": {
                "type": "string",
                "format": "file-path",
                "exists": true,
                "pattern": "^\\S+\\.(af|bcf|vcf|vcf.gz|gvcf|gvcf.gz)$",
                "errorMessage": "Data file cannot contain spaces and must be VCF/gVCF/BCF/gBCF"
            }
        },
        "required": ["sample", "datatype", "datafile"]
    }
}
